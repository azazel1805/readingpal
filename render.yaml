# render.yaml (Final, Simplified Version)

services:
  - type: web
    name: pronunciation-pal
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    # The envVars section has been completely removed.
