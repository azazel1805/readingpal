# render.yaml (Corrected Single Service Version)

services:
  - type: web
    name: pronunciation-pal
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      # THIS BLOCK IS NOW CORRECT
      - key: GEMINI_API_KEY
        fromSecret: true
